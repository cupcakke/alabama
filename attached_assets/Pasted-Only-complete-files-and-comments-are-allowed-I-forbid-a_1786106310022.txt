Only complete files and comments are allowed! I forbid any simplified, mock, placeholder, dummy, simulated, incomplete, or unoptimized production-ready code. It must not be truncated, and there must be no instances of … or similar. 50 xy must not be a dummy, "to do," or "sorry" placeholder. You must provide the complete code for every file; do not write anything else besides that.

Only complete files and comments are allowed! I forbid any simplified, mock, placeholder, dummy, simulated, incomplete, or unoptimized production-ready code. It must not be truncated, and there must be no instances of … or similar. 50 xy must not be a dummy, "to do," or "sorry" placeholder. You must provide the complete code for every file; do not write anything else besides that.

tehát valyu platformon indítok később kutatást és ahhoz fogom ezt az mcp-t csatlakoztatni hogy elérje az md fájlok tartalmait

the complete, production-ready codebase for a remote HTTP tool server in Python that will be deployed on Replit and connected as a custom MCP server inside Valyu DeepResearch. Build the entire system from scratch and output every required file in full, with no omissions, no placeholders, no mocks, no stubs, no pseudo-code, no fake implementations, and no TODO markers. The result must be directly deployable and usable as a public MCP endpoint.

System objective: Create a public remote MCP server that exposes tools for reading and searching Markdown documentation files stored on disk. The server will be deployed on Replit, will run continuously as an HTTP service, and will be connected to Valyu DeepResearch through the mcp_servers configuration using a public HTTPS URL. The server must allow unrestricted public access with no authentication. Its main purpose is to let a research agent search across all Markdown files instantly and read any matching file in full.

Deployment context:

The implementation language must be Python.
The server must be compatible with deployment on Replit.
The server must run as a long-lived HTTP service, not a local stdio-only process.
The server must bind to 0.0.0.0 and read the listening port from the PORT environment variable, with a sensible fallback such as 8000 when PORT is not defined.
The public endpoint path must be /mcp so that the final deployed URL is usable in Valyu as an MCP server URL.
The server must require no bearer token, no custom headers, and no other authentication layer, because the endpoint is intentionally public.
Valyu integration requirements: The code and output must explicitly support use inside Valyu DeepResearch through an MCP server entry equivalent to this structure:

mcp_servers is an array
each object includes url, name, tool_prefix, auth, and optional allowed_tools
auth must work with type set to none The server must therefore expose tools in a way that a remote MCP client can discover and call over HTTP from Valyu. The final output must also include a short example of the exact Valyu-side configuration snippet showing how to register the deployed server URL with auth.type set to none and a tool prefix such as docs.
Data model and storage rules:

Markdown files are stored locally on disk inside a directory named docs.
The server must read the docs directory at runtime so it always reflects current files on disk.
Every file ending in .md inside docs must be considered available content.
No markdown file in docs should be hidden, excluded, filtered, redacted, truncated, or summarized.
File contents must be returned exactly as stored, using UTF-8.
Required project files: Produce the complete content of every file needed to run this on Replit. At minimum include:

server.py as the main application file.
requirements.txt with all required Python dependencies.
.replit if needed for correct startup behavior.
replit.nix only if required by the chosen approach.
A minimal docs directory example only if necessary to demonstrate expected structure, but do not use fake business content; if sample files are included, they must be plainly marked as structural examples.
Any helper module only if it serves a real implementation purpose. Do not split files unnecessarily.
Protocol and framework requirements:

Use a Python framework or library that supports building a real MCP-compatible remote HTTP server.
Do not build a custom fake protocol. The server must expose actual callable MCP tools over HTTP transport.
The implementation must be suitable for a remote MCP client such as Valyu DeepResearch.
The main script must launch the server directly when executed.
The implementation must be production-ready in structure, not a toy example.
Tool requirements: Implement the following callable tools exactly and fully.

Tool 1: list_markdown_files Purpose: Return all available Markdown files. Behavior:

Scan the docs directory each time the tool is called.
Return every filename ending in .md.
Do not cache results unless clearly justified and fully implemented.
Do not exclude any matching file.
Return a clean machine-readable structure.
Tool 2: read_markdown_file Purpose: Return the entire contents of a specified Markdown file. Behavior:

Accept a filename string parameter.
Resolve the path safely inside docs.
Open the file with UTF-8 encoding.
Return the complete file contents exactly as stored.
If the file does not exist, return a proper error through the server framework in a clean, explicit way.
Do not summarize, transform, shorten, sanitize, or preprocess the content.
Tool 3: search_markdown Purpose: Perform full-text literal search across all Markdown files and return every occurrence from every matching file in a single response. Behavior:

Accept a required query string.
Accept an optional case_sensitive boolean defaulting to false.
Accept an optional context_lines integer defaulting to 1.
Search every .md file in docs with no exceptions.
Treat the query as a literal string, not a regex pattern, even if it contains regex characters.
Search line by line through each file.
For every matching line, capture:
the 1-based line number
the exact matching line text
a context snippet containing the requested number of lines before and after the match
Return all matches, not just the first match per file.
Continue until every line of every markdown file has been checked.
Exclude non-matching files from the results.
Return a structured object containing:
query
total_files_matched
total_matches
results keyed by filename
for each filename: match_count and matches
Each match entry must include line_number, match_line, and context.
The implementation must be efficient, deterministic, and easy for an LLM client to consume.
Implementation quality requirements:

Use clear type annotations everywhere practical.
Use docstrings on every tool so remote clients can understand their purpose.
Validate inputs where necessary.
Handle common runtime failures cleanly.
Ensure path handling cannot escape the docs directory.
Keep the code concise but complete.
Do not include unnecessary abstractions or architecture that complicates deployment.
Do not include authentication, rate limiting, databases, background queues, analytics, or unrelated infrastructure unless essential.
Replit deployment requirements:

The output must include the exact run configuration needed for Replit.
The service must start correctly on Replit without manual editing after file creation.
The implementation must work in a standard Replit Python environment.
Explain how the docs directory should be placed in the project tree.
Include the exact command Replit should run to start the service.
Ensure the server stays compatible with Replit Deployment rather than only the temporary interactive run button.
Valyu usage example: After the code files, include an exact example JSON object showing how the deployed server would be attached inside Valyu DeepResearch through mcp_servers, using:

a realistic placeholder HTTPS URL ending in /mcp
name set to github_docs or docs
tool_prefix set to docs
auth.type set to none
optionally allowed_tools containing list_markdown_files, read_markdown_file, and search_markdown This example must match the capabilities of the implemented server.
Output format requirements:

Output every file in full.
Separate files clearly with filenames as headings.
Do not omit any code.
Do not provide a summary instead of code.
Do not compress the response.
Do not replace real implementations with descriptions.
After the files, include a short deployment section with the precise Replit steps and the precise Valyu MCP registration snippet.
Final constraint: The result must be a real, fully working, public, HTTP-based Python MCP server that can be deployed on Replit, pointed to by a public HTTPS URL, and connected directly into Valyu DeepResearch as a custom MCP server so the agent can list Markdown files, read full Markdown files, and search instantly across all Markdown files stored in docs.